[project]
name = "siteindexer-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
    "trafilatura>=2.0.0",
]
[tool.uv]
package = true
